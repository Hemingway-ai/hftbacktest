# HftBacktest 项目启动指南

## 目录

- [项目简介](#项目简介)
- [项目架构总览](#项目架构总览)
- [环境要求](#环境要求)
- [安装与构建](#安装与构建)
- [数据准备](#数据准备)
- [快速开始：Python 回测](#快速开始python-回测)
- [快速开始：Rust 回测](#快速开始rust-回测)
- [数据采集器 (Collector)](#数据采集器-collector)
- [实盘交易 (Live Trading)](#实盘交易-live-trading)
- [项目目录结构详解](#项目目录结构详解)
- [构建配置说明](#构建配置说明)
- [常用教程与示例](#常用教程与示例)
- [常见问题](#常见问题)
- [参考资源](#参考资源)

---

## 项目简介

**HftBacktest** 是一个专为高频交易 (HFT) 和做市策略开发设计的回测框架。其核心特点：

- **逐笔模拟 (Tick-by-Tick)**：基于完整的订单簿和逐笔成交数据进行市场回放
- **延迟建模**：同时考虑行情延迟 (Feed Latency) 和订单延迟 (Order Latency)
- **队列位置模拟**：模拟限价单在订单簿中的排队位置，提供更真实的成交模拟
- **Rust + Python 混合架构**：Rust 提供高性能核心引擎，Python 提供便捷的策略开发接口
- **回测与实盘统一**：同一套策略代码可同时用于回测和实盘交易

**支持的交易所**：Binance Futures、Binance Spot、Bybit

**许可证**：MIT

---

## 项目架构总览

```
┌─────────────────────────────────────────────────────────┐
│                    用户策略层                              │
│         Python (Numba JIT)  /  Rust                      │
├──────────────┬──────────────┬───────────────────────────┤
│   回测引擎    │   实盘交易    │      数据采集              │
│  (Backtest)  │   (Live)     │    (Collector)             │
├──────────────┴──────────────┴───────────────────────────┤
│              核心库 hftbacktest (Rust)                    │
│  ┌─────────┐ ┌──────────┐ ┌──────────┐ ┌─────────────┐ │
│  │ 订单簿   │ │ 延迟模型  │ │ 队列模型  │ │  费率模型    │ │
│  │ (Depth) │ │(Latency) │ │ (Queue)  │ │   (Fee)     │ │
│  └─────────┘ └──────────┘ └──────────┘ └─────────────┘ │
├─────────────────────────────────────────────────────────┤
│           连接器 (Connector) - IPC / iceoryx2            │
│     Binance Futures │ Binance Spot │ Bybit               │
└─────────────────────────────────────────────────────────┘
```

---

## 环境要求

### 必需

| 依赖项 | 最低版本 | 说明 |
|--------|---------|------|
| **Python** | 3.11+ | 支持 3.11 / 3.12 / 3.13 |
| **Rust** | 1.91.1+ | 仅在从源码构建时需要 |
| **pip** | 最新版 | Python 包管理器 |

### Python 依赖

| 包名 | 版本要求 | 用途 |
|------|---------|------|
| numpy | >=2.0, <2.3 | 数值计算 |
| numba | ~0.61 | JIT 编译加速策略 |
| polars | 最新 | 数据处理 |
| matplotlib | 最新 | 可视化 |
| holoviews | 最新 | 交互式可视化 |

### 可选依赖

| 包名 | 用途 |
|------|------|
| databento | Databento 数据源支持 |
| maturin ~1.7 | 从源码构建 Python 绑定 |

---

## 安装与构建

### 方式一：pip 安装（推荐，仅使用 Python）

```bash
pip install hftbacktest
```

如需 Databento 数据源支持：

```bash
pip install hftbacktest[databento]
```

### 方式二：从源码构建 Python 包

适用于需要修改底层 Rust 代码或使用最新开发版本的场景。

```bash
# 1. 克隆仓库
git clone https://github.com/nkaz001/hftbacktest
cd hftbacktest

# 2. 确保已安装 Rust 工具链 (>= 1.91.1)
rustc --version

# 3. 安装 maturin 构建工具
pip install maturin~=1.7

# 4. 进入 Python 绑定目录，开发模式安装
cd py-hftbacktest
pip install -e .

# 或构建 release wheel
maturin build --release
pip install target/wheels/hftbacktest-*.whl
```

### 方式三：仅构建 Rust 库

适用于纯 Rust 策略开发。

```bash
cd hftbacktest

# 开发构建
cargo build

# Release 构建（全优化 + LTO）
cargo build --release

# 仅构建回测功能（不含实盘）
cargo build --no-default-features --features backtest

# 仅构建实盘功能
cargo build --no-default-features --features live

# 构建含 S3 数据源支持
cargo build --features s3
```

### 构建数据采集器

```bash
cargo build -p collector --release
```

### 构建实盘连接器

```bash
# 构建所有交易所连接器（默认）
cargo build -p connector --release

# 仅构建特定交易所
cargo build -p connector --release --no-default-features --features binancefutures
cargo build -p connector --release --no-default-features --features bybit
```

---

## 数据准备

HftBacktest 使用 **NPZ 格式**（NumPy 压缩二进制）存储行情数据。框架提供了多种数据源的转换工具。

### 支持的数据源

| 数据源 | 转换工具模块 |
|--------|-------------|
| Binance Futures | `hftbacktest.data.utils.binancefutures` |
| Binance 历史数据 | `hftbacktest.data.utils.binancehistmktdata` |
| Bybit | `hftbacktest.data.utils.bybit` |
| Bybit 历史数据 | `hftbacktest.data.utils.bybithistmktdata` |
| Tardis | `hftbacktest.data.utils.tardis` |
| Databento | `hftbacktest.data.utils.databento` |
| Hyperliquid | `hftbacktest.data.utils.hyperliquid` |
| MEXC | `hftbacktest.data.utils.mexc` |

### 数据准备流程（以 Tardis 为例）

项目在 `hftbacktest/examples/` 下提供了完整的 Python 数据准备流水线：

```bash
# 步骤 1：配置交易对列表
# 编辑 hftbacktest/examples/tickers.json

# 步骤 2：从 Tardis 下载原始数据
python hftbacktest/examples/2_download_tardis.py

# 步骤 3：转换为 NPZ 格式
python hftbacktest/examples/3_convert.py

# 步骤 4：生成延迟数据（可选）
python hftbacktest/examples/4_latency.py
```

### 公开数据源

项目支持者提供了部分公开数据，可在以下地址获取：
`https://reach.stratosphere.capital/data/usdm/`

### 项目自带示例数据

`examples/usdm/` 目录下包含 BTC/ETH 的示例数据文件，可直接用于运行示例 Notebook。

---

## 快速开始：Python 回测

### 基本回测示例

```python
from numba import njit
import numpy as np
from hftbacktest import (
    HashMapMarketDepthBacktest,
    BUY, SELL, GTX, LIMIT,
)

@njit
def my_strategy(hbt):
    asset_no = 0
    tick_size = hbt.depth(asset_no).tick_size
    lot_size = hbt.depth(asset_no).lot_size

    # 每 10ms 运行一次
    while hbt.elapse(10_000_000) == 0:
        hbt.clear_inactive_orders(asset_no)

        depth = hbt.depth(asset_no)
        mid_price = (depth.best_bid + depth.best_ask) / 2.0
        position = hbt.position(asset_no)

        # 简单做市策略：在 best bid/ask 挂单
        order_qty = np.round(100 / mid_price / lot_size) * lot_size

        if position <= 0:
            hbt.submit_buy_order(
                asset_no, 1, depth.best_bid_tick * tick_size,
                order_qty, GTX, LIMIT, False
            )
        if position >= 0:
            hbt.submit_sell_order(
                asset_no, 2, depth.best_ask_tick * tick_size,
                order_qty, GTX, LIMIT, False
            )

        if not hbt.wait_order_response(asset_no, -1, 5_000_000_000):
            return False
    return True

# 配置并运行回测（具体参数参见教程）
# hbt = HashMapMarketDepthBacktest(assets)
# my_strategy(hbt)
```

### 运行完整示例

```bash
# 运行回测示例
python hftbacktest/examples/5_backtest.py

# 运行参数网格搜索
python hftbacktest/examples/6_gridsearch.py
```

### 使用 Jupyter Notebook

项目提供了 22 个详细的 Jupyter Notebook 教程，位于 `examples/` 目录：

```bash
cd examples
jupyter notebook "Getting Started.ipynb"
```

---

## 快速开始：Rust 回测

### 运行内置示例

```bash
# 运行网格交易回测示例（需要先准备好数据文件）
cargo run --release --example gridtrading_backtest
```

### Rust 回测代码结构

一个典型的 Rust 回测程序包含以下部分：

```rust
use hftbacktest::prelude::*;
use hftbacktest::backtest::{
    Backtest, ExchangeKind, L2AssetBuilder,
    assettype::LinearAsset,
    data::{DataSource, read_npz_file},
    models::{
        CommonFees, IntpOrderLatency,
        PowerProbQueueFunc3, ProbQueueModel,
        TradingValueFeeModel,
    },
    recorder::BacktestRecorder,
};

fn main() {
    tracing_subscriber::fmt::init();

    // 1. 配置延迟数据
    let latency_data = (20240501..20240532)
        .map(|date| DataSource::File(format!("latency_{date}.npz")))
        .collect();
    let latency_model = IntpOrderLatency::new(latency_data, 0);

    // 2. 配置资产类型、队列模型
    let asset_type = LinearAsset::new(1.0);
    let queue_model = ProbQueueModel::new(PowerProbQueueFunc3::new(3.0));

    // 3. 加载行情数据
    let data = (20240501..20240532)
        .map(|date| DataSource::File(format!("1000SHIBUSDT_{date}.npz")))
        .collect();

    // 4. 构建回测引擎
    let hbt = Backtest::builder()
        .add_asset(
            L2AssetBuilder::new()
                .data(data)
                .latency_model(latency_model)
                .asset_type(asset_type)
                .fee_model(TradingValueFeeModel::new(
                    CommonFees::new(-0.00005, 0.0007)
                ))
                .exchange(ExchangeKind::NoPartialFillExchange)
                .queue_model(queue_model)
                .depth(|| {
                    let mut depth = HashMapMarketDepth::new(0.000001, 1.0);
                    depth.apply_snapshot(
                        &read_npz_file("1000SHIBUSDT_20240501_SOD.npz", "data")
                            .unwrap(),
                    );
                    depth
                })
                .build()
                .unwrap(),
        )
        .build()
        .unwrap();

    // 5. 运行策略并记录结果
    let mut recorder = BacktestRecorder::new(&hbt);
    // gridtrading(&mut hbt, &mut recorder, ...);
    // recorder.to_csv("result", ".").unwrap();
}
```

---

## 数据采集器 (Collector)

Collector 是一个独立的 Rust 程序，用于从交易所实时采集行情数据并保存为 NPZ 格式。

### 支持的交易所

- Binance Spot
- Binance USDM Futures
- Binance COIN-M Futures
- Bybit
- Hyperliquid

### 构建与运行

```bash
# 构建
cargo build -p collector --release

# 运行（具体参数参见 collector 的 CLI 帮助）
./target/release/collector --help
```

---

## 实盘交易 (Live Trading)

> **注意**：实盘交易功能目前仅支持 Rust。

### 架构说明

实盘交易采用 **Bot + Connector** 分离架构，通过 IPC (iceoryx2) 通信：

```
┌──────────┐    IPC (iceoryx2)    ┌─────────────┐
│  Bot     │ ◄──────────────────► │  Connector  │
│ (策略)   │                      │ (交易所连接) │
└──────────┘                      └─────────────┘
```

- **Bot**：运行交易策略，与回测使用相同的算法代码
- **Connector**：负责与交易所的 WebSocket/REST 通信，支持多个 Bot 共享连接

### 运行实盘

```bash
# 1. 启动 Connector（以 Binance Futures 为例）
cargo run -p connector --release -- --config config.toml

# 2. 运行策略 Bot
cargo run --release --example gridtrading_live
```

### Rust 实盘示例

项目提供了以下实盘示例：

| 文件 | 说明 |
|------|------|
| `hftbacktest/examples/gridtrading_live.rs` | Binance Futures 网格交易 |
| `hftbacktest/examples/gridtrading_live_bybit.rs` | Bybit 网格交易 |
| `hftbacktest/examples/live_order_error_handling.rs` | 实盘订单错误处理 |
| `hftbacktest/examples/logging_order_latency.rs` | 订单延迟日志记录 |

---

## 项目目录结构详解

```
hftbacktest/
├── hftbacktest/                  # [Rust] 核心库
│   ├── src/
│   │   ├── lib.rs               # 库入口
│   │   ├── types.rs             # 核心类型定义
│   │   ├── prelude.rs           # 常用导出
│   │   ├── backtest/            # 回测引擎
│   │   │   ├── mod.rs           # 回测主模块
│   │   │   ├── order.rs         # 订单数据结构
│   │   │   ├── state.rs         # 回测状态管理
│   │   │   ├── assettype.rs     # 资产类型 (线性/反向合约)
│   │   │   ├── proc/            # 交易所模拟处理器
│   │   │   ├── data/            # 数据读取 (NPZ/NPY)
│   │   │   └── models/          # 模型 (延迟/队列/费率)
│   │   ├── depth/               # 订单簿实现
│   │   │   ├── btreemarketdepth.rs    # BTreeMap 实现
│   │   │   ├── hashmapmarketdepth.rs  # HashMap 实现（通用）
│   │   │   └── roivectormarketdepth.rs # Vector 实现（高性能）
│   │   └── live/                # 实盘交易
│   │       ├── bot.rs           # 交易机器人
│   │       └── ipc/             # IPC 通信 (iceoryx2)
│   └── examples/                # Rust 示例
│       ├── algo.rs              # 网格交易算法
│       ├── gridtrading_backtest.rs
│       ├── gridtrading_live.rs
│       └── ...
│
├── hftbacktest-derive/           # [Rust] 过程宏
│
├── py-hftbacktest/               # [Python] Python 绑定
│   ├── src/                     # Rust FFI 层 (PyO3)
│   ├── hftbacktest/             # Python 包
│   │   ├── __init__.py
│   │   ├── binding.py           # 类型提示与绑定
│   │   ├── data/utils/          # 数据转换工具
│   │   │   ├── binancefutures.py
│   │   │   ├── bybit.py
│   │   │   ├── tardis.py
│   │   │   └── ...
│   │   └── stats/               # 绩效统计
│   └── tests/
│
├── collector/                    # [Rust] 行情数据采集器
│   └── src/
│       ├── main.rs
│       ├── binance/
│       ├── bybit/
│       └── hyperliquid/
│
├── connector/                    # [Rust] 实盘交易连接器
│   └── src/
│       ├── main.rs
│       ├── binancefutures/
│       ├── binancespot/
│       └── bybit/
│
├── examples/                     # Jupyter Notebook 教程 (22个)
│   ├── Getting Started.ipynb
│   ├── Data Preparation.ipynb
│   ├── High-Frequency Grid Trading.ipynb
│   └── ...
│
├── docs/                         # Sphinx 文档源码
├── Cargo.toml                    # Rust 工作空间配置
├── pyproject.toml                # Python 包配置
└── ROADMAP.md                    # 开发路线图
```

---

## 构建配置说明

### Rust Feature Flags

**核心库 (`hftbacktest`)**：

| Feature | 默认 | 说明 |
|---------|------|------|
| `backtest` | 是 | 回测引擎（含 NPZ 读取、UUID 等） |
| `live` | 是 | 实盘交易（含 Tokio、iceoryx2 等） |
| `s3` | 否 | AWS S3 数据源支持 |

**连接器 (`connector`)**：

| Feature | 默认 | 说明 |
|---------|------|------|
| `binancefutures` | 是 | Binance USDM Futures |
| `binancespot` | 是 | Binance Spot |
| `bybit` | 是 | Bybit |

### Cargo 构建 Profile

| Profile | 优化级别 | LTO | 用途 |
|---------|---------|-----|------|
| `dev` | 0 | 否 | 日常开发 |
| `test` | 0 | 否 | 测试 |
| `release` | 3 | 是 | 生产部署（全优化 + strip） |
| `release-with-debug` | 3 | 是 | 性能分析（保留调试符号） |

### macOS Apple Silicon 注意事项

项目已在 `.cargo/config.toml` 中配置了 `aarch64-apple-darwin` 的链接器参数，无需额外配置。

---

## 常用教程与示例

### Jupyter Notebook 教程（推荐学习路径）

| 序号 | 教程 | 内容 |
|------|------|------|
| 1 | Data Preparation | 数据下载与格式转换 |
| 2 | Getting Started | 框架基础入门 |
| 3 | Working with Market Depth and Trades | 订单簿与成交数据操作 |
| 4 | High-Frequency Grid Trading | 高频网格交易策略 |
| 5 | GLFT Market Making Model | Guéant-Lehalle-Fernandez-Tapia 做市模型 |
| 6 | Impact of Order Latency | 订单延迟对策略的影响 |
| 7 | Market Making with Alpha - OBI | 基于订单簿不平衡的做市 |
| 8 | Market Making with Alpha - APT | 基于 APT 模型的做市 |
| 9 | Market Making with Alpha - Basis | 基于基差的做市 |
| 10 | Queue-Based Market Making | 基于队列位置的做市 |
| 11 | Accelerated Backtesting | 加速回测（Rust 后端） |
| 12 | Pricing Framework | 定价框架 |

### Python 示例流水线

```
1_ticker.py → 2_download_tardis.py → 3_convert.py → 4_latency.py → 5_backtest.py → 6_gridsearch.py
  配置交易对      下载原始数据          转换格式        生成延迟数据     运行回测         参数优化
```

---

## 常见问题

### Q: Numba 编译很慢怎么办？

首次运行时 Numba 需要 JIT 编译策略函数，后续运行会使用缓存。可参考 `Accelerated Backtesting.ipynb` 教程，使用 Rust 后端获得更好的性能。

### Q: 如何选择订单簿实现？

| 实现 | 适用场景 |
|------|---------|
| `HashMapMarketDepth` | 通用场景，灵活性好 |
| `BTreeMapMarketDepth` | 需要有序遍历的场景 |
| `ROIVectorMarketDepth` | 已知价格范围，追求极致性能 |

### Q: 回测和实盘的代码能复用吗？

可以。框架设计了统一的 `Bot` trait，回测引擎和实盘 Bot 都实现了该接口，策略代码无需修改即可在两种模式下运行。

### Q: 如何从 V1 迁移到 V2？

请参考官方迁移指南：`https://hftbacktest.readthedocs.io/en/latest/migration2.html`

### Q: 支持哪些操作系统？

- Linux (x86_64, aarch64, armv7)
- macOS (aarch64 Apple Silicon, x86_64 Intel)
- Windows (x64)

---

## 参考资源

| 资源 | 链接 |
|------|------|
| 官方文档 | https://hftbacktest.readthedocs.io/ |
| GitHub 仓库 | https://github.com/nkaz001/hftbacktest |
| PyPI | https://pypi.org/project/hftbacktest |
| crates.io | https://crates.io/crates/hftbacktest |
| Rust API 文档 | https://docs.rs/hftbacktest/ |
| 开发路线图 | [ROADMAP.md](./ROADMAP.md) |
